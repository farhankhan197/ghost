#include "diff.hpp"
#include <vector>

namespace ghost {
namespace git {

std::vector<DiffFile> Diff::getChangedFiles(const std::string& range) {
    std::vector<DiffFile> result;
    return result;
}

}
}